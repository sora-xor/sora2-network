# SCCP Roadmap

Last updated: 2026-02-16

## Latest tasks

- [x] Added a Vue 3 SCCP cross-chain UI widget for TON swap app (`/Users/mtakemiya/dev/tonswap_web/components/Crosschain.vue`).
- [x] Wired TON swap cross-chain navigation (`navItems` + `mobileTabs`) and i18n (`nav.crosschain`, `crosschain.*`) in `/Users/mtakemiya/dev/tonswap_web`.
- [x] Added Solswap Vue 3 SCCP cross-chain view and route (`/crosschain`) with shared BSC/ETH/TRON payload builder and copy flow (`/Users/mtakemiya/dev/solswap/apps/web/src/views/CrosschainView.vue`).
- [x] Added Solswap route-level navigation/i18n wiring for SCCP (`/Users/mtakemiya/dev/solswap/apps/web/src/{router.ts,App.vue,i18n/locales/en.json}`).
- [x] Verified Polkaswap web already had SCCP route/view in `/Users/mtakemiya/dev/polkaswap-exchange-web/src/{router/modules/bridge.ts,views/Sccp.vue,lang/*}`; no additional UI tasks pending there.
- [ ] Optional follow-up: localize SCCP `crosschain.*` strings in non-English locales for Solswap and Tonswap (`i18n.ts` + locale bundles).

## Completed

- [x] Extracted `default_required_domains_for_bound` and reused it for canonical SCCP core-domain defaults with bounded safe push behavior.
- [x] Centralized required-domain validation into `normalize_required_domains` and reused it in governance updates and genesis build.
- [x] Hardened `set_required_domains`:
  - [x] Canonical sorted storage.
  - [x] Rejection of duplicates, unsupported domains, SORA domain, and missing core domains.
  - [x] Non-manager origin rejection.
  - [x] No mutation and no event emission on failed updates.
- [x] Required domains policy decision completed for first release:
  - [x] `RequiredDomains` is constrained to exactly SCCP core domains (ETH/BSC/SOL/TON/TRON).
  - [x] Enforced uniformly in governance updates and genesis build validation.
- [x] Hardened genesis config handling:
  - [x] Canonicalization at genesis build.
  - [x] Fail-fast panic on invalid `required_domains`.
- [x] Aligned SCCP pallet versioning for first release (`v1` only):
  - [x] Storage version set to `1`.
  - [x] Removed runtime migration/backward-compatibility upgrade path assumptions.
- [x] Hardened fail-closed activation behavior under storage corruption:
  - [x] Core domains still required even if `RequiredDomains` is corrupted.
  - [x] Unknown injected/corrupted domains are rejected.
- [x] Expanded pallet tests (`pallets/sccp/src/tests.rs`) for helper behavior, genesis validation, governance failure paths, and corruption fail-closed activation.
- [x] Expanded runtime integration tests (`runtime/src/tests/sccp_runtime_integration.rs`) for default domains, canonical state/event hash, all rejection paths, and no side effects on failures.
- [x] Expanded runtime integration tests for token activation prerequisites:
  - [x] Activation fails when only a partial core-domain remote-token configuration exists.
  - [x] Activation fails when only a partial core-domain endpoint configuration exists.
  - [x] Activation succeeds when all core-domain remote-token + endpoint prerequisites are configured.
- [x] Expanded runtime integration tests for domain-specific identifier length validation:
  - [x] `set_remote_token` rejects 32-byte value for EVM-like ETH domain (expects 20 bytes).
  - [x] `set_remote_token` rejects 20-byte value for SOL domain (expects 32 bytes).
  - [x] `set_domain_endpoint` rejects 32-byte value for TRON domain (expects 20 bytes).
  - [x] `set_domain_endpoint` rejects 20-byte value for TON domain (expects 32 bytes).
- [x] Expanded runtime integration tests for governance/domain validation on token/domain registration:
  - [x] `set_remote_token` rejects SORA domain and unknown domain ids.
  - [x] `set_domain_endpoint` rejects SORA domain and unknown domain ids.
  - [x] `set_remote_token` and `set_domain_endpoint` reject non-manager origins.
- [x] Expanded runtime integration tests for `set_remote_token` / `set_domain_endpoint` behavior:
  - [x] `set_remote_token` rejects `TokenNotFound` and emits no event on failure.
  - [x] `set_domain_endpoint` emits no event on failure.
  - [x] `set_remote_token` emits `RemoteTokenSet` with expected `id_hash`.
  - [x] `set_domain_endpoint` emits `DomainEndpointSet` with expected `id_hash`.
- [x] Expanded runtime integration tests for domain-endpoint clear governance:
  - [x] `clear_domain_endpoint` clears stored endpoint and emits `DomainEndpointCleared`.
  - [x] `clear_domain_endpoint` rejects `SORA`/unknown domains.
  - [x] `clear_domain_endpoint` rejects non-manager origin.
  - [x] Failed `clear_domain_endpoint` emits no event.
- [x] Expanded runtime integration tests for activation gating after endpoint removal:
  - [x] Clearing a core-domain endpoint after registration causes activation to fail with `DomainEndpointMissing`.
- [x] Expanded runtime integration tests for BSC light-client governance guardrails:
  - [x] `init_bsc_light_client` rejects non-manager origin.
  - [x] `init_bsc_light_client` rejects oversized checkpoint headers (`BscHeaderTooLarge`).
  - [x] Failed `init_bsc_light_client` emits no event.
  - [x] `submit_bsc_header` rejects when BSC light-client state is not initialized.
  - [x] `submit_bsc_header` rejects oversized headers (`BscHeaderTooLarge`).
  - [x] Failed `submit_bsc_header` emits no event.
  - [x] `set_bsc_validators` canonicalizes storage and emits `BscValidatorsUpdated` with expected hash.
  - [x] `set_bsc_validators` rejects duplicate entries and non-manager origin.
  - [x] Failed `set_bsc_validators` emits no event.
- [x] Expanded runtime integration tests for TRON light-client governance guardrails:
  - [x] `init_tron_light_client` rejects non-manager origin.
  - [x] `init_tron_light_client` rejects oversized checkpoint raw data (`TronHeaderTooLarge`).
  - [x] `init_tron_light_client` rejects invalid witness-signature length (`TronHeaderInvalid`).
  - [x] Failed `init_tron_light_client` emits no event.
  - [x] `submit_tron_header` rejects when TRON light-client state is not initialized.
  - [x] `submit_tron_header` rejects oversized raw data (`TronHeaderTooLarge`).
  - [x] `submit_tron_header` rejects invalid witness-signature length (`TronHeaderInvalid`).
  - [x] Failed `submit_tron_header` emits no event.
  - [x] `set_tron_witnesses` canonicalizes storage and emits `TronWitnessesUpdated` with expected hash.
  - [x] `set_tron_witnesses` rejects duplicate entries and non-manager origin.
  - [x] Failed `set_tron_witnesses` emits no event.
- [x] Expanded runtime integration tests for BSC/TRON light-client success paths:
  - [x] `init_bsc_light_client` accepts a real BSC header fixture and sets expected head/finalized state.
  - [x] `submit_bsc_header` imports a valid linear extension, updates finalized state, and emits `BscHeaderImported`.
  - [x] `init_tron_light_client` accepts a synthetic checkpoint header and sets expected head/finalized state.
  - [x] `submit_tron_header` imports a valid linear extension and emits `TronHeaderImported`.
  - [x] `init_bsc_light_client` and `init_tron_light_client` success tests assert emitted initialization/finalized events (and TRON witnesses-hash event).
- [x] Expanded runtime integration tests for `mint_from_proof` guardrails:
  - [x] Payload/domain sanity checks reject unsupported destination/source combinations.
  - [x] Unknown source domain is rejected.
  - [x] Missing source-domain endpoint is rejected (`DomainEndpointMissing`).
  - [x] Failed `mint_from_proof` emits no event.
- [x] Expanded runtime integration tests for `attest_burn` guardrails:
  - [x] Payload/domain sanity checks reject unsupported source/destination combinations.
  - [x] Unknown source domain is rejected.
  - [x] Non-canonical EVM destination recipient is rejected (`RecipientNotCanonical`).
  - [x] Zero amount and zero recipient are rejected before endpoint/proof paths.
  - [x] Failed `attest_burn` emits no event.
- [x] Expanded runtime integration tests for `burn` guardrails and lifecycle:
  - [x] Zero amount / zero recipient are rejected.
  - [x] Destination-domain validation enforced (`SORA`/unknown domains rejected).
  - [x] Non-canonical EVM recipient is rejected.
  - [x] Outbound pause control is enforced (`OutboundDomainPaused`).
  - [x] Missing destination endpoint is rejected (`DomainEndpointMissing`).
  - [x] Token-state gating enforced (`TokenNotFound`, `TokenNotActive`).
  - [x] Successful burn records increment nonce, emit `SccpBurned`, and persist burn record.
  - [x] Failed `burn` emits no event.
- [x] Expanded runtime integration tests for signed-origin enforcement on permissionless SCCP calls:
  - [x] `burn` rejects unsigned (`root`) origin.
  - [x] `mint_from_proof` rejects unsigned (`root`) origin.
  - [x] `attest_burn` rejects unsigned (`root`) origin.
  - [x] `submit_bsc_header` rejects unsigned (`root`) origin.
  - [x] `submit_tron_header` rejects unsigned (`root`) origin.
- [x] Expanded runtime integration tests for inbound/outbound incident-control and fail-closed finality on proof entrypoints:
  - [x] `mint_from_proof` rejects when source finality is unavailable (`InboundFinalityUnavailable`) once endpoint checks pass.
  - [x] `mint_from_proof` enforces inbound pause control (`InboundDomainPaused`) with available finality mode.
  - [x] `attest_burn` enforces inbound pause control (`InboundDomainPaused`).
  - [x] `attest_burn` enforces outbound pause control (`OutboundDomainPaused`).
  - [x] `attest_burn` rejects when source finality is unavailable (`InboundFinalityUnavailable`) once endpoint checks pass.
- [x] Expanded runtime integration tests for proof-path success and replay guardrails:
  - [x] `mint_from_proof` (attester quorum mode) marks `processed_inbound` and emits `SccpMinted` on success.
  - [x] `mint_from_proof` replay is rejected with `InboundAlreadyProcessed` and emits no event.
  - [x] `attest_burn` (attester quorum mode) marks `attested_outbound` and emits `SccpBurnAttested` on success.
  - [x] `attest_burn` replay is rejected with `BurnAlreadyAttested` and emits no event.
- [x] Expanded runtime integration tests for token lifecycle governance guardrails:
  - [x] `add_token` rejects non-manager origin and duplicate registration (`TokenAlreadyExists`).
  - [x] `activate_token` rejects non-manager origin, missing token (`TokenNotFound`), and non-pending token (`TokenNotPending`).
  - [x] `remove_token` rejects non-manager origin and missing token (`TokenNotFound`).
  - [x] `finalize_remove` rejects non-manager origin, missing token (`TokenNotFound`), and non-removing token (`TokenNotRemoving`).
  - [x] Failed lifecycle-governance calls emit no events.
- [x] Runtime integration coverage now includes every SCCP pallet extrinsic (`v1` scope), with targeted success and/or fail-closed rejection-path assertions.
- [x] Expanded runtime integration tests for token removal lifecycle:
  - [x] `remove_token` + `finalize_remove` clear token state and per-domain remote token ids.
  - [x] `finalize_remove` enforces grace-period gating (`GracePeriodNotExpired` when finalized too early).
  - [x] `remove_token` and `finalize_remove` emit expected lifecycle events.
- [x] Expanded runtime integration tests for inbound grace-period governance:
  - [x] `set_inbound_grace_period` updates storage and emits `InboundGracePeriodSet`.
  - [x] `set_inbound_grace_period` rejects non-manager origin.
  - [x] Failed `set_inbound_grace_period` call emits no event and does not mutate state.
- [x] Expanded runtime integration tests for incident-control pause governance:
  - [x] `set_inbound_domain_paused` updates state and emits `InboundDomainPausedSet`.
  - [x] `set_outbound_domain_paused` updates state and emits `OutboundDomainPausedSet`.
  - [x] Domain validation enforced (`SORA`/unknown domains rejected).
  - [x] Non-manager origin rejected for inbound/outbound pause controls.
  - [x] Failed pause-control governance calls emit no events.
- [x] Expanded runtime integration tests for inbound-message invalidation governance:
  - [x] `invalidate_inbound_message` updates state and emits `InboundMessageInvalidated`.
  - [x] `clear_invalidated_inbound_message` clears state and emits `InboundMessageRevalidated`.
  - [x] Domain validation enforced (`SORA`/unknown domains rejected where applicable).
  - [x] Non-manager origin rejected for invalidate/clear operations.
  - [x] Failed invalidate/clear operations emit no events.
- [x] Expanded runtime integration tests for governance validation symmetry:
  - [x] `set_inbound_domain_paused` rejects unknown domains (in addition to SORA).
  - [x] `set_outbound_domain_paused` rejects SORA domain (in addition to unknown domains).
  - [x] `invalidate_inbound_message` rejects unknown domains (in addition to SORA).
  - [x] `clear_invalidated_inbound_message` rejects SORA domain (in addition to unknown domains).
- [x] Expanded runtime integration tests for governance idempotency/overwrite behavior:
  - [x] Inbound/outbound pause controls support true->false roundtrip updates.
  - [x] `set_inbound_finality_mode` override can be updated and persists latest value.
  - [x] `set_remote_token` overwrite updates stored id and latest event hash.
  - [x] `set_domain_endpoint` overwrite updates stored id and latest event hash.
- [x] Expanded runtime integration tests for inbound attester-quorum governance:
  - [x] `set_inbound_attesters` stores canonical sorted set + threshold and emits hashed event.
  - [x] `set_inbound_attesters` rejects duplicates and invalid thresholds.
  - [x] `set_inbound_attesters` enforces domain/origin validation and emits no event on failure.
  - [x] `clear_inbound_attesters` clears set+threshold, emits event, and enforces domain/origin validation.
  - [x] Failed `clear_inbound_attesters` emits no event.
- [x] Expanded runtime integration tests for inbound finality-mode governance:
  - [x] `set_inbound_finality_mode` updates per-domain override and emits `InboundFinalityModeSet`.
  - [x] Unsupported mode/domain combinations are rejected (`InboundFinalityModeUnsupported`).
  - [x] Domain validation enforced (`SORA`/unknown domains rejected).
  - [x] Non-manager origin rejected.
  - [x] Failed finality-mode governance calls emit no events.
- [x] Extended `ExtBuilder` to support custom genesis required domains for focused tests (`with_required_domains`).
- [x] Hardened SCCP matrix runner (`misc/sccp/run_all_tests.sh`) with:
  - [x] Runtime SCCP subset execution.
  - [x] Required sibling-repo directory checks.
  - [x] Solana controls (`SOLANA_TEST_RUST_LOG`, `SCCP_SOL_PROGRAM_RETRIES`, `SCCP_SOL_PROGRAM_TEST_THREADS`, `SCCP_SOL_PROGRAM_RETRY_DELAY_SECS`, `SCCP_SOL_PROGRAM_TIMEOUT_SECS`, `SCCP_SOL_PROGRAM_LOG_DIR`, `SCCP_SOL_PROGRAM_LOG_TAIL_LINES`, `SCCP_SOL_PROGRAM_PRESERVE_LOGS`, `SCCP_SOL_PROGRAM_NOCAPTURE`, `SCCP_SOL_PROGRAM_ALLOW_FAILURE`).
  - [x] Env value validation, retry-delay pacing, optional per-attempt timeout support, per-attempt log capture with failure tail excerpts, focused single-test rerun hints, and reproduce command on exhaustion.
  - [x] CI fail-closed enforcement (`CI=1|true` rejects `SCCP_SOL_PROGRAM_ALLOW_FAILURE=1`).
- [x] Updated SCCP docs (`docs/sccp/README.md`) for required-domain hardening and matrix-runner behavior.
- [x] Added generated SCCP matrix log path to `.gitignore` (`misc/sccp/logs/`) to keep working tree clean.
- [x] Added dedicated Solana flake stress harness (`misc/sccp/stress_sccp_sol_program.sh`) with:
  - [x] Multi-iteration loop, optional test filter, timeout support, delay pacing, and stop-on-first-failure mode.
  - [x] Structured summary output (including parsed failure details) and per-iteration failure diagnostics with focused rerun hints.
- [x] Stabilized known flaky Solana incident-controls flow in sibling repo test (`../sccp-sol/program/tests/sccp_flow.rs`) by isolating the paused-failure burn probe to a dedicated burn-record PDA (`blocked_burn_rec`) so the post-unpause success path uses a distinct transaction/account context.

## Validation (Latest Local Run)

- [x] `cargo test -p sccp -- --nocapture` passed (`52 passed`, `0 failed`).
- [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`36 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after activation test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`39 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after length-validation test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`43 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after governance/domain validation test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`49 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after event/failure-path test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`54 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after removal-lifecycle test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`56 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after grace-period governance test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`59 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after pause-governance test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`67 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after invalidation-governance test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`75 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after finality-mode governance test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`81 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after governance-symmetry test expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`85 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after governance overwrite/idempotency expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`90 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after attester-governance expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`98 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after endpoint-clear governance expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`115 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after BSC/TRON governance guardrail expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`127 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after BSC/TRON protocol-bound validation expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`133 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after `mint_from_proof` / `attest_burn` guardrail expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`147 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after `burn` guardrail/lifecycle expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`158 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after signed-origin guardrail expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`163 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after proof-path incident/finality guardrail expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`168 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after lifecycle-governance guardrail expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`182 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after lifecycle-success/event-coverage expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`186 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after proof success/replay expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`190 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after BSC/TRON light-client success-path expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`193 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after BSC submit-success expansion:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`194 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after light-client init event-assertion hardening:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`194 passed`, `0 failed`).
- [x] Runtime SCCP integration suite rerun after light-client failure-no-event hardening:
  - [x] `cargo test -p framenode-runtime sccp_ -- --nocapture` passed (`198 passed`, `0 failed`).
- [x] SCCP pallet suite rerun after lifecycle-governance guardrail expansion:
  - [x] `cargo test -p sccp -- --nocapture` passed (`52 passed`, `0 failed`).
- [x] `bash -n misc/sccp/run_all_tests.sh` passed.
- [x] Full cross-repo matrix passed:
  - [x] `SCCP_SOL_PROGRAM_ALLOW_FAILURE=1 SCCP_SOL_PROGRAM_RETRIES=2 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=3 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] `sccp-sol/program` failed on attempt `1/2` with transient `TransactionError(InstructionError(0, Custom(25)))`, then passed on attempt `2/2`.
  - [x] Attempt logs captured under `misc/sccp/logs/`.
- [x] Solana stress sample run completed:
  - [x] `SCCP_SOL_STRESS_RUNS=6 SCCP_SOL_STRESS_DELAY_SECS=1 SCCP_SOL_STRESS_ALLOW_FAILURE=1 SCCP_SOL_STRESS_PRESERVE_PASS_LOGS=0 bash misc/sccp/stress_sccp_sol_program.sh`
  - [x] Result: `passes=6`, `fails=0`; summary at `misc/sccp/logs/stress/sccp-sol-program-stress.20260215-155819.summary.txt`.
- [x] Higher-iteration Solana stress run completed:
  - [x] `SCCP_SOL_STRESS_RUNS=20 SCCP_SOL_STRESS_DELAY_SECS=0 SCCP_SOL_STRESS_ALLOW_FAILURE=1 SCCP_SOL_STRESS_PRESERVE_PASS_LOGS=0 bash misc/sccp/stress_sccp_sol_program.sh`
  - [x] Result: `passes=11`, `fails=9` (45% failure rate) in `24s`; summary at `misc/sccp/logs/stress/sccp-sol-program-stress.20260215-155953.summary.txt`.
  - [x] All observed failures mapped to `solana_program_flow_burn_and_mint_with_incident_controls` with `TransactionError(InstructionError(0, Custom(25)))` (`Custom(25)` maps to `OutboundDomainPaused` in `../sccp-sol/program/src/lib.rs`).
- [x] Hotspot-only stress run completed (filtered to known flaky test):
  - [x] `SCCP_SOL_STRESS_RUNS=20 SCCP_SOL_STRESS_DELAY_SECS=0 SCCP_SOL_STRESS_ALLOW_FAILURE=1 SCCP_SOL_STRESS_PRESERVE_PASS_LOGS=0 SCCP_SOL_STRESS_TEST_FILTER=solana_program_flow_burn_and_mint_with_incident_controls bash misc/sccp/stress_sccp_sol_program.sh`
  - [x] Result: `passes=13`, `fails=7` (35% failure rate) in `12s`; summary at `misc/sccp/logs/stress/sccp-sol-program-stress.20260215-160132.summary.txt`.
  - [x] Parsed failure details consistently show `TransactionError(InstructionError(0, Custom(25)))` on the same test.
- [x] Post-fix hotspot-only stress run completed (same filter):
  - [x] `SCCP_SOL_STRESS_RUNS=20 SCCP_SOL_STRESS_DELAY_SECS=0 SCCP_SOL_STRESS_ALLOW_FAILURE=1 SCCP_SOL_STRESS_PRESERVE_PASS_LOGS=0 SCCP_SOL_STRESS_TEST_FILTER=solana_program_flow_burn_and_mint_with_incident_controls bash misc/sccp/stress_sccp_sol_program.sh`
  - [x] Result: `passes=20`, `fails=0`; summary at `misc/sccp/logs/stress/sccp-sol-program-stress.20260215-160537.summary.txt`.
- [x] Post-fix full SCCP matrix run completed with strict Solana settings (no non-blocking mode):
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed; `sccp-sol/program` passed on first attempt.
- [x] Post-fix unfiltered Solana stress run completed:
  - [x] `SCCP_SOL_STRESS_RUNS=20 SCCP_SOL_STRESS_DELAY_SECS=0 SCCP_SOL_STRESS_ALLOW_FAILURE=1 SCCP_SOL_STRESS_PRESERVE_PASS_LOGS=0 bash misc/sccp/stress_sccp_sol_program.sh`
  - [x] Result: `passes=20`, `fails=0`; summary at `misc/sccp/logs/stress/sccp-sol-program-stress.20260215-160614.summary.txt`.
- [x] Extended post-fix hotspot soak run completed:
  - [x] `SCCP_SOL_STRESS_RUNS=100 SCCP_SOL_STRESS_DELAY_SECS=0 SCCP_SOL_STRESS_ALLOW_FAILURE=1 SCCP_SOL_STRESS_PRESERVE_PASS_LOGS=0 SCCP_SOL_STRESS_TEST_FILTER=solana_program_flow_burn_and_mint_with_incident_controls bash misc/sccp/stress_sccp_sol_program.sh`
  - [x] Result: `passes=100`, `fails=0`; summary at `misc/sccp/logs/stress/sccp-sol-program-stress.20260215-160838.summary.txt`.
- [x] Extended strict full-matrix soak completed (3 consecutive runs):
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh` executed 3x consecutively.
  - [x] Result: all `3/3` runs passed; logs at `misc/sccp/logs/matrix-soak-20260215-1610/`.
- [x] Post-runtime-expansion strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `163 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260215-224112.attempt-1.log`.
- [x] Post-proof-guardrail strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `168 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260215-224315.attempt-1.log`.
- [x] Post-lifecycle-guardrail strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `182 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260215-233653.attempt-1.log`.
- [x] Post-coverage strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `186 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260215-235031.attempt-1.log`.
- [x] Post-proof-success strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `190 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260216-093638.attempt-1.log`.
- [x] Post-light-client-success strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `193 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260216-094052.attempt-1.log`.
- [x] Post-BSC-submit-success strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `194 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260216-094645.attempt-1.log`.
- [x] Post-init-event-assertion strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `194 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260216-094942.attempt-1.log`.
- [x] Post-light-client-failure-no-event strict full SCCP matrix rerun completed:
  - [x] `SCCP_SOL_PROGRAM_RETRIES=1 SCCP_SOL_PROGRAM_TEST_THREADS=1 SCCP_SOL_PROGRAM_RETRY_DELAY_SECS=0 SCCP_SOL_PROGRAM_PRESERVE_LOGS=1 SCCP_SOL_PROGRAM_LOG_TAIL_LINES=120 bash misc/sccp/run_all_tests.sh`
  - [x] Result: full matrix passed (including runtime SCCP subset at `198 passed`, `0 failed`); Solana program logs captured at `misc/sccp/logs/sccp-sol-program.20260216-095303.attempt-1.log`.

## Current Known Risk

- [x] Prior flake in `solana_program_flow_burn_and_mint_with_incident_controls` (`TransactionError(InstructionError(0, Custom(25)))` mapped to `OutboundDomainPaused`) has not reproduced in extended post-fix reruns (`100/100` hotspot stress, `20/20` unfiltered stress, strict matrix `3/3`).
- [x] Matrix runner supports retries and optional non-blocking mode (`SCCP_SOL_PROGRAM_ALLOW_FAILURE=1`) as operational safety controls when needed.
- [x] Matrix runner is fail-closed in CI even when local non-blocking mode exists (`SCCP_SOL_PROGRAM_ALLOW_FAILURE=1` is rejected when `CI=1|true`).
- [x] Additional stabilization controls are now available (`SCCP_SOL_PROGRAM_RETRY_DELAY_SECS`, `SCCP_SOL_PROGRAM_TIMEOUT_SECS`, `SCCP_SOL_PROGRAM_LOG_DIR`, `SCCP_SOL_PROGRAM_LOG_TAIL_LINES`, `SCCP_SOL_PROGRAM_PRESERVE_LOGS`, `SCCP_SOL_PROGRAM_NOCAPTURE`).
- [x] No remaining local feature/test gaps identified for SCCP `v1` scope across this repo and sibling test matrix.

## Next

- [ ] Optional external follow-up only: run/observe CI matrix for sustained stability.
