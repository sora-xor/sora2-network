#!/bin/sh

#cargo run --release --features include-real-files -- build-spec --chain main-coded --raw > node/src/chain_spec/bytes/chain_spec_main.json
#cargo run --release --features "private-net include-real-files reduced-pswap-reward-periods" -- build-spec --chain test-coded --raw > node/src/chain_spec/bytes/chain_spec_test.json
#cargo run --release --features "private-net include-real-files" -- build-spec --chain staging-coded --raw > node/src/chain_spec/bytes/chain_spec_staging.json